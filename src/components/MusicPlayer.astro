---
// APlayer + MetingJS via CDN
// Optional: replace `playlistId` with your own netease cloud playlist ID,
// or remove the <MusicPlayer /> import from Footer.astro to disable the player entirely.
// Default below is a public ACG playlist used as a demo.
const playlistServer = 'netease';
const playlistType = 'playlist';
const playlistId = '2619366951';
---

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/aplayer@1.10.1/dist/APlayer.min.css" />
<script is:inline src="https://cdn.jsdelivr.net/npm/aplayer@1.10.1/dist/APlayer.min.js"></script>
<script is:inline src="https://cdn.jsdelivr.net/npm/meting@2.0.1/dist/Meting.min.js"></script>

<meting-js
	server={playlistServer}
	type={playlistType}
	id={playlistId}
	fixed="true"
	mini="true"
	autoplay="false"
	preload="none"
	theme="#2337ff"
	loop="all"
	order="random"
	volume="0.5"
	list-folded="true"
></meting-js>

<style is:global>
	/* APlayer mini fixed 模式样式调整 */
	.aplayer.aplayer-fixed {
		z-index: 99 !important;
		left: auto !important;
		right: 20px !important;
		bottom: 20px !important;
	}
	.aplayer.aplayer-fixed .aplayer-body {
		left: -66px !important;
	}
	/* 在小屏幕上让播放器更小一点 */
	@media (max-width: 640px) {
		.aplayer.aplayer-fixed {
			right: 10px !important;
			bottom: 10px !important;
		}
	}
</style>
